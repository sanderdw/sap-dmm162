column table "MAP::ADDR_MAP" ( LOGSYS NVARCHAR(10) not null,
	 NODE_KEY VARBINARY(16) not null,
	 ADDRESSID BIGINT generated BY DEFAULT AS IDENTITY ( start with 9000000000 increment by 1 ) not null,
	 -- optional: MAXVALUE, CYCLE, CACHE parameter
 primary key cpbtree (LOGSYS,
	 NODE_KEY) )
